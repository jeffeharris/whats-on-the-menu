interface:
  display_name: "Pickup"
  short_description: "Resume safely from a branch handoff"
  default_prompt: "Use $pickup to verify this branch handoff and propose the next execution plan."

policy:
  allow_implicit_invocation: false
